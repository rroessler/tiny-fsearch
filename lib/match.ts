/** Match Functionality. */
export namespace Match {
    //  TYPEDEFS  //

    /** Match Result Interface. */
    export interface IResult {
        line: number;
        column: number;
        content: string;
    }
}
